# M3: Project + 命令 + 网络

目标：能保存/恢复完整工作环境，支持网络调试。

## 交付清单
1. **Project 系统**（PRJ 全部）：.maxcomprj 读写（原子写）+ 生命周期 + 会话恢复 + 导出文件夹 + 配置优先级。
2. **命令系统**（CMD 全部）：命令列表/快捷发送/变量替换/定时发送/自动校验/命令链。
3. **TCP/UDP**（TP-T01/06/07）。
4. **文件保存/发送**（LOG-T06）。
5. **XYZmodem**（协议层）。

## 退出标准（验收演示脚本）
- 建一个调试环境（串口配置 + 命令 + 过滤 + 绘图）→ 存 .maxcomprj → 关闭重开 → 一键恢复自动连接。
- 命令列表发送 AT 指令，变量替换 + 自动校验生效。
- TCP 客户端连本地 echo server 收发正确。
- 数据落盘含时间戳。

## 进度门：M3 通过前不进入 M4。
