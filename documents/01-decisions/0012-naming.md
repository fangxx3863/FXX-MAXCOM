# ADR-0012: 命名 — MAXCOM

状态: accepted | 日期: 2026-07-31

## 背景
产品需要一个代号与正式名。

## 决策
代号 **MAXCOM**，正式名待定。代码包名 `fxx_maxcom`，可执行名 `MAXCOM`。

## 理由
- 简洁、与串口/网络调试的定位贴合。
- 正式名可后续更换，不影响内部代号。

## 后果
- 目录结构、包名、可执行文件名以 MAXCOM 为准（见 02-contracts / 03-modules）。
