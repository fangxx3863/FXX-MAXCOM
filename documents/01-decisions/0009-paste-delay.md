# ADR-0009: 粘贴处理 — 逐行发送 + 可配置延迟

状态: accepted | 日期: 2026-07-31

## 背景
交互式终端需支持粘贴多行文本，但一次性批量发送可能超出远端串口缓冲或打乱行序。

## 决策
粘贴采用**逐行发送 + 可配置延迟**（默认 10ms/行），逐字符或批量发送可配置。

## 理由
- 兼顾速度与可靠，避免远端缓冲溢出。
- 延迟可配覆盖不同串口速率场景。

## 后果
- 终端 Viewport 键盘处理需支持粘贴事件 + 分片发送队列。
