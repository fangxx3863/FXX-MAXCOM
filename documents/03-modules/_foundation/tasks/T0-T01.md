# T0-T01: 项目骨架 + pyproject + CI

> 模块：_foundation ｜ 依赖：无 ｜ **项目第一个任务**

## 目标
搭实现仓骨架：`pyproject.toml`（唯一依赖白名单）、目录结构、GitHub Actions CI。

## IMPL `[详细]`

### 文件结构
- `pyproject.toml` — **唯一依赖白名单**，精确版本（非 ^/~），照 tech-stack-lock §1
- `uv.lock` — uv 锁文件（`uv lock` 生成）
- `src/` 目录骨架（见 SPEC §2）
- `.github/workflows/ci.yml` — lint + format-check + check-deps + typecheck + test + build

### 易错点
- **依赖锁精确版本**（`dearpygui==1.x.y` 不是 `dearpygui>=1`）:版本浮动 = 不可复现。
- `pyproject.toml` 的 `[tool.ruff]`、`[tool.mypy]`、`[tool.pytest]` 配置与 05-quality 一致。
- 只加 tech-stack-lock 内依赖，禁止自己装库。

### 完成标准（DoD）
- [ ] `uv sync` 成功；`uv run pytest` 在空骨架通过（一个冒烟测试）
- [ ] CI 文件就位，lint/typecheck/test job 定义完整
- [ ] 目录结构符合 SPEC §2

## 禁止事项
- 不写业务逻辑；不加 tech-stack-lock 外的依赖
