# FLT-T03: 过滤行落盘策略（O2 待确认）

> 模块：filter ｜ 依赖：FLT-T01 ｜ 状态：待确认（O2）

## 目标
确定被过滤行是否写入文件。（O2 尚未拍板：倾向默认**不写**，保留"落盘"选项。若确认，实现对应策略。）

## 待确认内容
- 被过滤（隐藏）的行，文件导出/日志轮转时是否包含？
- 方案 A：不写（倾向，V1）；方案 B：写但标记（用于回溯）；方案 C：可配置（UI 勾选）。

## IMPL（方案 A 定案后）`[骨架]`
- 文件落盘在日志引擎内，读取 should_show 判定：False 则跳过写盘。
- 与日志轮转/导出共享同一判定逻辑。

### 完成标准（DoD）
- [ ] 定案后实现对应策略
- [ ] 与显示过滤一致的落盘行为
