{
  "name": "maxcom-app",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/check-selectors.mjs && tsc --noEmit && vite build && node scripts/smoke.mjs && node scripts/plot-test.mjs && node scripts/modbus-test.mjs",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/cli": "^2.11.4",
    "@tauri-apps/plugin-dialog": "^2.7.2",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "typescript": "^7.0.2",
    "uplot": "^1.6.32",
    "vite": "^8.2.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "playwright-core": "^1.62.1"
  }
}
